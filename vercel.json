{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "build": {
    "env": {
      "PRISMA_GENERATE": "true"
    }
  },
  "buildCommand": "npx prisma generate && next build"
}
