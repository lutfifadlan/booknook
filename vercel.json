{
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/next"
    }
  ],
  "build": {
    "env": {
      "PRISMA_GENERATE": "true"
    }
  },
  "buildCommand": "prisma generate && next build"
}
